import React from 'react';
import { Typography } from '@mui/material';

const SplashScreen = () => {
    return (
        <div>
            <Typography variant="h1">Sleep Tracker</Typography>
            <Typography variant="body1">Track your sleep for better health!</Typography>
        </div>
    );
};

export default SplashScreen;
